{% extends 'base.html.twig' %}

{% block body %}
    <h1>Connexion</h1>
    <form action="{{ path('security_login') }}" method="post">
        <div class="form-group">
            <input placeholder="Adresse email..." required name="_username"
                   type="text" class="form-control">
        </div>
        <div class="form-group">
            <input placeholder="mot de passe..." required name="_password"
                   type="password" class="form-control">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success">Connexion</button>
        </div>

        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
        <div class="text-center">
		                        	<span class="d-sm-none d-md-none d-lg-none d-xl-none">
			                        	<a class="text-center" href="{{path('security_registration')}}">Inscription</a>
			                        	<br><br>
									</span>
            <a class="text-center" href="{{path('security_forgotPassword')}}">Mot de passe oublié ?</a>
            <br><br>
        </div>
    </form>
{% endblock %}
